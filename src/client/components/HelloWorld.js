import React from 'react';

const HelloWorld = () => {
  return (
    <div>
      <div>Hello hi asad world</div>
      <button onClick={() => console.log('clicked on button')}>Click me</button>
    </div>
  );
};

export default HelloWorld;
